---
management:
  security:
    enabled: false

security:
  basic:
    enabled: false
spring:
  application:
    name: company
  cloud:
    config:
      uri: ${vcap.services.config-service.credentials.uri}